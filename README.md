# TaskApp

Simple Spring Boot REST API for task management using JPA and UUID identifiers.

## UI \(Docker\)

If the UI is provided as a Docker image, run it locally (replace `your-registry/taskapp-ui:latest` with the actual image name):

## Tech
- Java 17+  
- Spring Boot  
- Maven  
- Jakarta Persistence (JPA)

## Requirements
- JDK 17+  
- Maven